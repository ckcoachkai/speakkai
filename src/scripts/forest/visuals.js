// Native Canvas sprite compositor and hierarchical 2D dog rig.
export const COLORS=[['Original',null],['Red','#d74740'],['Orange','#ed9235'],['Yellow','#efd04e'],['Lime','#a5cb47'],['Green','#3d9c69'],['Teal','#36b6af'],['Blue','#4a83d1'],['Navy','#304777'],['Purple','#9359c4'],['Pink','#e78daf'],['White','#e4e7dd'],['Charcoal','#50565b']];
export const MOODS=['Happy','Sad','Shocked'];
function surface(w,h){const c=document.createElement('canvas');c.width=w;c.height=h;return c;}
export function expressionCells(image){const out=[];for(let k=0;k<3;k++){const size=image.width/3,c=surface(360,360),x=c.getContext('2d');x.drawImage(image,k*size,0,size,image.height,0,0,360,360);const d=x.getImageData(0,0,360,360),a=d.data,seen=new Uint8Array(360*360),q=[];const push=i=>{if(i<0||i>=seen.length||seen[i])return;seen[i]=1;const p=i*4,r=a[p],g=a[p+1],b=a[p+2];if(Math.min(r,g,b)>118&&Math.max(r,g,b)-Math.min(r,g,b)<42){a[p+3]=0;q.push(i);}};for(let n=0;n<360;n++){push(n);push(359*360+n);push(n*360);push(n*360+359);}for(let j=0;j<q.length;j++){const i=q[j];if(i%360)push(i-1);if(i%360<359)push(i+1);push(i-360);push(i+360);}x.putImageData(d,0,0);out.push(c);}return out;}
export function dress(image,index,color){if(!color)return image;const c=surface(image.width,image.height),x=c.getContext('2d');x.drawImage(image,0,0);const d=x.getImageData(0,0,c.width,c.height),a=d.data;const target=[1,3,5].map(i=>parseInt(color.slice(i,i+2),16));for(let y=Math.floor(c.height*.19);y<c.height*.515;y++){for(let px=Math.floor(c.width*.17);px<c.width*.83;px++){const n=(y*c.width+px)*4,r=a[n],g=a[n+1],b=a[n+2];if(a[n+3]<100)continue;const skin=r>g*1.12&&g>b*1.08&&r>90;let cloth=false;if(index===0)cloth=Math.max(r,g,b)-Math.min(r,g,b)<55;else if(index===1)cloth=g>r*1.1&&g>b*.95;else if(index===2)cloth=!skin&&r>b*.94&&(r>g*1.08||Math.max(r,g,b)-Math.min(r,g,b)<25);else if(index===3)cloth=b>g*1.09&&b>r*.94;else cloth=Math.max(r,g,b)-Math.min(r,g,b)<27&&r>90;if(!cloth)continue;const lum=(r*.25+g*.6+b*.15)/255,shade=.33+lum*.75;for(let z=0;z<3;z++)a[n+z]=Math.min(255,target[z]*shade);}}x.putImageData(d,0,0);return c;}
// Limbs use nested shoulder/elbow/wrist and hip/knee/ankle transforms.
export function drawStudent(ctx,body,face,w,h,t=0,running=false,boost=false,gentle=false){
 const phase=t*(boost?19:12), strength=running?(gentle?.35:1.3):.12;
 function segment(length,width,color,texture=false){ctx.save();ctx.beginPath();ctx.roundRect(-width/2,-width*.3,width,length+width*.6,width/2);ctx.clip();ctx.fillStyle=color;ctx.fillRect(-width/2,-width*.3,width,length+width*.6);if(texture)ctx.drawImage(body,body.width*.43,body.height*.26,body.width*.13,body.height*.19,-width/2,-width*.3,width,length+width*.6);ctx.restore();}
 function leg(side){ctx.save();ctx.translate(side*w*.065,-h*.49);ctx.rotate(Math.sin(phase+side*1.57)*1.05*strength);segment(h*.22,w*.15,'#34516b');ctx.translate(0,h*.22);ctx.rotate((.3+Math.max(0,Math.cos(phase+side*1.57))*1.5)*strength);segment(h*.21,w*.12,'#294258');ctx.translate(0,h*.21);ctx.rotate(-.15+Math.sin(phase)*.22*strength);ctx.fillStyle='#e8e9e2';ctx.beginPath();ctx.roundRect(-w*.06,-h*.02,w*.22,h*.045,h*.02);ctx.fill();ctx.restore();}
 function arm(side){ctx.save();ctx.translate(side*w*.11,-h*.73);ctx.rotate((Math.sin(phase+side*1.57)*1.65+side*.4)*strength);segment(h*.14,w*.095,'#718096',true);ctx.translate(0,h*.14);ctx.rotate((Math.sin(phase*1.3+side)*1.45-.5)*strength);segment(h*.13,w*.075,'#d6a37f');ctx.translate(0,h*.13);ctx.rotate(Math.sin(phase*1.7)*.8*strength);ctx.fillStyle='#e4b18d';ctx.beginPath();ctx.ellipse(0,h*.018,w*.047,h*.034,0,0,Math.PI*2);ctx.fill();ctx.restore();}
 leg(-1);arm(-1);leg(1);
 ctx.save();ctx.beginPath();ctx.moveTo(-w*.13,-h*.8);ctx.lineTo(w*.13,-h*.8);ctx.lineTo(w*.16,-h*.48);ctx.lineTo(-w*.16,-h*.48);ctx.closePath();ctx.clip();ctx.drawImage(body,body.width*.37,body.height*.20,body.width*.27,body.height*.31,-w*.16,-h*.8,w*.32,h*.32);ctx.restore();arm(1);
 const size=h*.245;ctx.drawImage(face,-size*.48,-h*.995,size,size);
}
function flamingEyes(ctx,w,h,t){for(const [px,py,size] of [[-.223,-.853,1],[-.329,-.861,.48]]){ctx.save();ctx.translate(w*px,h*py);ctx.globalCompositeOperation='screen';ctx.shadowColor='#ff1000';ctx.shadowBlur=25;for(let k=0;k<5;k++){const flicker=Math.sin(t*13+k*2.1),x=(k-2)*5*size;ctx.fillStyle=k%2?'#ff7a16':'#ff2010';ctx.beginPath();ctx.moveTo(x-7*size,3);ctx.quadraticCurveTo(x-12*size,-13*size,x+flicker*9*size,-(24+12*Math.sin(t*9+k))*size);ctx.quadraticCurveTo(x+13*size,-10*size,x+7*size,3);ctx.fill();}ctx.fillStyle='#ff1607';ctx.beginPath();ctx.ellipse(0,0,12*size,6*size,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff0a1';ctx.beginPath();ctx.ellipse(0,0,4*size,5*size,0,0,Math.PI*2);ctx.fill();ctx.restore();}}

function cut(image,box){const c=surface(box[2],box[3]),x=c.getContext('2d');x.beginPath();x.roundRect(0,0,c.width,c.height,Math.min(c.width,c.height)*.44);x.clip();x.drawImage(image,...box,0,0,c.width,c.height);return c;}
export function prepareDog(art){return {upper:cut(art.dog,[315,775,160,310]),lower:cut(art.dog,[287,1040,160,320]),paw:cut(art.dog,[231,1360,205,140])};}
export function dogPose(action,t){const beat=t*Math.PI*2/1.25;let jump=0,sway=0,angles=[.08,-.10,.03,-.12,.18,-.04];if(action==='wave'){angles=[-1.75,.7+Math.sin(beat*1.5)*.6,Math.sin(beat*2)*.25,-.25,.3,0];sway=Math.sin(beat*.5)*.018;}if(action==='dance'){angles=[-1.2+Math.sin(beat)*.65,-.5+Math.cos(beat)*.55,Math.sin(beat)*.3,1.1+Math.sin(beat+1)*.65,.5+Math.cos(beat+1)*.55,Math.sin(beat+2)*.3];sway=Math.sin(beat)*.055;jump=Math.abs(Math.sin(beat*.5))*18;}if(action==='jump'){const p=t%1.7/1.7;jump=Math.sin(Math.PI*p)*100;angles=[-1.9,.1,.1,1.8,-.1,-.1];sway=Math.sin(beat*.5)*.025;}if(action==='stretch')angles=[-2.45,-.15,.1,2.45,.15,-.1];return {jump,sway,angles};}
export function drawDog(ctx,art,parts,box,pose,open,t=0){const {x,y,w,h}=box;ctx.save();ctx.translate(x+w*.5,y+h-pose.jump);ctx.rotate(pose.sway);ctx.drawImage(art['dog-base-v3'],-w/2,-h,w,h);if(!open){ctx.save();ctx.beginPath();ctx.ellipse(-w*.28,-h*.76,w*.19,h*.11,0,0,Math.PI*2);ctx.clip();ctx.drawImage(art['dog-closed'],-w/2,-h,w,h);ctx.restore();}flamingEyes(ctx,w,h,t);const endpoints=[];for(let arm=0;arm<2;arm++){const sx=arm===0?-w*.19:w*.005,sy=-h*.48;const a=pose.angles.slice(arm*3,arm*3+3),upper=h*.185,lower=h*.19;ctx.save();ctx.translate(sx,sy);ctx.rotate(a[0]);ctx.drawImage(parts.upper,-w*.075,-h*.035,w*.15,upper+h*.075);ctx.translate(0,upper);ctx.rotate(a[1]);ctx.drawImage(parts.lower,-w*.061,-h*.035,w*.122,lower+h*.065);ctx.translate(0,lower);ctx.rotate(a[2]);ctx.drawImage(parts.paw,-w*.079,-h*.025,w*.158,h*.091);ctx.restore();endpoints.push({x:sx-Math.sin(a[0])*upper-Math.sin(a[0]+a[1])*lower,y:sy+Math.cos(a[0])*upper+Math.cos(a[0]+a[1])*lower});}ctx.restore();return endpoints;}
export function drawBanana(ctx,x,y,t){ctx.save();ctx.translate(x,y);ctx.shadowColor='#ffe958';ctx.shadowBlur=20;ctx.fillStyle='#fce65733';ctx.beginPath();ctx.ellipse(0,6,39,11,0,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0;ctx.rotate(-.28);const gradient=ctx.createLinearGradient(0,-40,0,0);gradient.addColorStop(0,'#fff8a1');gradient.addColorStop(.5,'#ffdc27');gradient.addColorStop(1,'#e9a914');ctx.fillStyle=gradient;ctx.strokeStyle='#8f610c';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(-35,-27);ctx.bezierCurveTo(-20,19,30,19,40,-32);ctx.bezierCurveTo(13,-8,-12,-3,-35,-27);ctx.fill();ctx.stroke();ctx.fillStyle='#665024';ctx.fillRect(34,-36,8,9);ctx.restore();ctx.font='bold 20px Arial';ctx.textAlign='center';ctx.fillStyle='#fff5a4';ctx.strokeStyle='#193422';ctx.lineWidth=5;ctx.strokeText('2×',x,y-47);ctx.fillText('2×',x,y-47);}
