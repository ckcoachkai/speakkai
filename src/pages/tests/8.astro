---
import EditableOriginalPage from "../../components/experiments-v4/EditableOriginalPage.astro";
import { experimentV4ByNumber } from "../../data/experimentLabV4";

const experiment = experimentV4ByNumber.get(4);
if (!experiment) throw new Error("Original Version 4 is missing.");
---
<EditableOriginalPage experiment={experiment} version={8} />
