---
import EditableOriginalPage from "../../components/experiments-v4/EditableOriginalPage.astro";
import { experimentV4ByNumber } from "../../data/experimentLabV4";

const experiment = experimentV4ByNumber.get(1);
if (!experiment) throw new Error("Original Version 1 is missing.");
---

<EditableOriginalPage experiment={experiment} version={5} />
