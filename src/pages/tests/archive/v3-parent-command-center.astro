---
import V3ExperimentLabPage from "../../../components/archive/V3ExperimentLabPage.astro";
import { experimentById } from "../../../data/experimentLabV3Archive";

const experiment = experimentById.get(40);

if (!experiment) {
  throw new Error("Archived V3 Test 40 is unavailable.");
}
---

<V3ExperimentLabPage experiment={experiment} />
