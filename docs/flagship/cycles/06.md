# Version 06 — Editable lessons and guided practice

Status: complete and live verified. Entering champion V05:39.5/55, live verified.

## Research before implementation — 2026-09-07

Official Keystatic local-mode and Astro production-admin recipe were refreshed (HTTP200). Local mode writes repository files; the documented pattern conditionally excludes admin routes from production. Package registry verifies @keystatic/astro6.0.0 accepts Astro5/6/7 and React18/19; @keystatic/core0.6.9 accepts React19. The installed pinned integration injects its two routes only in astro:config:setup. V06 wraps that hook to run only in development and requires an explicit local-editor launcher bound to127.0.0.1. Official Astro React integration supports islands without framework migration. China learning-loop research refresh informs editable audience, timing, steps and listener guidance. Prior social-platform limits remain unchanged.

Hypothesis: a real local CMS save can change the source used by the public static lesson, with unpublished entries and editor endpoints excluded from production; an optional React guide helps visitors follow one practice step at a time while preserving the full static sheet.

Dependency isolation: replaced this worktree's node_modules junction with its own pnpm installation, leaving the shared target untouched. Existing Astro5.18.2 retained. This also resolves the preview server's late-discovered font/dev-toolbar allowlist errors caused by the old junction. Recheck actual loaded fonts and responsive geometry in V06; prior local screenshots may have used fallback fonts. Public build assets were independently produced by CI.

## Completed implementation and checks

The one-object lesson is now authored in Keystatic JSON and rendered through Astro's validated collection. Public lesson pages, Resources and the sitemap all select published entries. A React island provides introduction, five steps, previous/next, completion and restart; the complete static printable sheet remains below it. Only title and steps enter island props. The homepage remains free of React hydration. Owner instructions are in CONTENT-EDITOR.md.

Local CMS verification: opened the actual editor, changed the introduction with a temporary sentence, clicked Save, confirmed the JSON changed and HTTP200 lesson HTML contained that exact sentence, then restored the wording through the editor. A temporary unpublished lesson fixture was excluded from its route, Resources and sitemap; its editor-only sentinel was absent from public output. The fixture was removed and final output rebuilt.

Build: 45 pages, zero errors/warnings, seven hints (existing structured-script, schedule and legacy audio deprecation hints). Eight flagship route checks pass with 139 local targets; eight metadata graphs and fourteen sitemap targets pass. Eighteen inquiry/Speak regressions and schedule privacy pass. Public content parity, draft filtering, editor-note exclusion and homepage island exclusion pass. Frozen pnpm install succeeds. CI now uses Node24 and pnpm11.19.0 with the committed lockfile, matching local validation.

Dev repairs: excluding only @keystatic/astro/api from browser dependency optimization resolves its server virtual-module scan error. The CMS uses a separate Vite cache; this fixes an observed development jsxDEV failure when a production check/build overwrote the shared cache. Fresh hydration after another build passed. No server module is globally externalized or mocked.

Chromium QA: keyboard Enter starts guide; next and previous preserve step order; completion, return to last step, restart and return to introduction pass. Each change focuses its heading. Mobile320 and desktop1440 show no horizontal overflow on homepage, Resources and the lesson, with one H1 per route. Mobile controls exceed48px height. Actual loaded font faces are Manrope400/600/700. Mobile guide and desktop guide/homepage screenshots reviewed. Print media hides the guide/navigation while retaining all five steps. JavaScript-disabled reload retains the entire lesson and removes inactive enhanced controls. Temporary print/script emulation was restored.

Performance limit: the lesson adds approximately62.9KB gzipped across the emitted React client, shared index and guide chunks, plus Astro's inline island bootstrap. The homepage does not load those chunks. This is an asset-size observation, not a field speed result. Other browser engines, real screen-reader behavior, physical print pagination, 200% text enlargement, learning outcomes and conversion remain unverified.

## Critique and champion comparison

What improved: content has a usable editing path, a tested publication filter and a visitor-controlled practice sequence. What feels average: the lesson introduction is long on mobile; only one activity exists; authoring is local and most marketing content remains outside the CMS. Ahead of general advice pages: the first-attempt/feedback/retry loop is immediately usable and remains printable. Behind leader libraries: depth, bilingual support, real demonstration media and attributable outcomes. No missing proof is replaced with invented testimonials.

Provisional scores in standard order: 3.5,2,4,4,4,4,3.5,3.5,3,4,4.5 =40/55 versus championV05 39.5/55. The sole numerical increase is maintainability, supported by real form-save-to-render verification and automated filtering. The guide adds observable functionality but does not establish a measured conversion or learning gain. Promotion requires independent review and successful live verification.

## Top five next improvements

1. Shorten the practice entry and bring the useful interaction closer on mobile.
2. Add a useful school/group activity through the CMS and verify multiple entries.
3. Add bilingual course and inquiry content with fact parity.
4. Add an adult/team practice activity and connect it to corporate inquiries.
5. Establish repeatable accessibility and asset/runtime budgets, including text enlargement.

Publication verified below; V07 may proceed.

Independent source review found no P0/P1/P2 blocker. Its P3 date-shape finding was fixed with semantic calendar-date validation. Both reviewer and coordinator score40/55; coordinator attributes the increase only to maintainability. A separate production preview completed all five React steps and finished successfully.

LIVE VERIFIED 2026-09-07: commit a5eeb4a6601814fc1bf408043925ee20516c573b; workflow34101111850 succeeded. All8 flagship routes returned200 and marker06. Public guide started, advanced through5 steps and finished; full sheet retained. ChampionV06, 6/30 complete.
